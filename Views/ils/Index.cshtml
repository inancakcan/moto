@model IEnumerable<moto.Models.il>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ilID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ilID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ilID })
        </td>
    </tr>
}

</table>
