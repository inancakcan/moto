@model IEnumerable<moto.Models.semt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ilce.ad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ilce.ad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.semtID }) |
            @Html.ActionLink("Details", "Details", new { id=item.semtID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.semtID })
        </td>
    </tr>
}

</table>
